#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# @file .config/husky/pre-commit
# @brief A git hook script for the `pre-commit` hook

echo "Performing various pre-commit tasks on staged files (like autofixing, detecting private keys, etc.)"
STAGED_FILES=$(git diff --cached --name-only)
for FILE in "$STAGED_FILES"; do
  if [ -f "$1" ]; then
    task git:hook:pre-commit -- "$FILE" &
  fi
done
wait

echo 'Linting and fixing the staged files with `lint-staged`'
task lint:lint-staged

echo 'Reporting possible spelling errors in the staged files with `cspell`'
task lint:spelling

echo 'Pre-commit validation complete'
