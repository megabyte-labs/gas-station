---
ansible_lint:
  stage: lint
  image: pipelinecomponents/ansible-lint
  tags:
    - docker
  script:
    - ansible-galaxy install -r requirements.yml
    - ansible-lint
  only:
    - branches
  except:
    - master
