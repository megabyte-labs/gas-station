---
lsif:go:
  allow_failure: true
  artifacts:
    reports:
      lsif: dump.lsif
  image: sourcegraph/lsif-go:latest
  needs: []
  rules:
    - if: $DO_CI == 'false'
      when: never
  script:
    - lsif-go
  stage: lsif
