---
markdown-spellcheck:
  stage: lint
  image: pipelinecomponents/markdown-spellcheck:latest
  tags:
    - docker
  script:
    - mdspell --report '**/*.md'
  only:
    - branches
  except:
    - master
