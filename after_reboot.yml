---
- name: Ensure Protected folder has appropriate permissions
  file:
    path: "/home/{{ ansible_user }}/Documents/Protected"
    state: directory
    owner: root
    group: sambausers
    mode: "770"
    force: yes