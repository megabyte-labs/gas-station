---
# These are the pip packages that are automatically installed on hosts in the desktop group
pip_packages:
  # @pypi [ansible-lint](https://pypi.org/project/ansible-lint/) - Lint tool that checks [Ansible](https://www.ansible.com/) projects for best practices and problematic code
  - name: ansible-lint

  # @pypi [asciinema](https://pypi.org/project/asciinema/) - Tool that records terminal session and replay them in a terminal as well as in a web browser
  - name: asciinema

  # @pypi [aws-shell](https://pypi.org/project/aws-shell/) - AWS shell is the interactive productivity booster for the AWS CLI
  - name: aws-shell

  # @pypi [cookiecutter](https://pypi.org/project/cookiecutter/) - A command-line utility that creates projects from cookiecutters (project templates)
  - name: cookiecutter

  # @pypi [gdown](https://pypi.org/project/gdown) - An alternative to wget and curl that can handle downloading large files from Google Drive
  - name: gdown

  # @pypi [gixy](https://pypi.org/project/gixy/) - A tool to analyze Nginx configuration
  - name: gixy

  # @pypi [httpstat](https://pypi.org/project/httpstat/) - A script that reflects curl statistics in a fascinating and well-defined way, it is a single file which is compatible with Python 3 and requires no additional software (dependencies) to be installed on a users system
  - name: httpstat

  # @pypi [http-prompt](https://pypi.org/project/http-prompt/) - An interactive command-line HTTP client featuring autocomplete and syntax highlighting, built on HTTPie and prompt_toolkit
  - name: http-prompt

  # @pypi [iredis](https://pypi.org/project/iredis/) - A terminal client for redis with auto-completion and syntax highlighting
  - name: iredis

  # @pypi [kube-shell](https://pypi.org/project/kube-shell/) - An integrated shell for working with the Kubernetes CLI
  - name: kube-shell

  # @pypi [litecli](https://pypi.org/project/litecli/) - A command-line client for SQLite databases that has auto-completion and syntax highlighting
  - name: litecli

  # @pypi [netaddr](https://pypi.org/project/netaddr/) - A system-independent network address manipulation library for Python 2.7 and 3.5+
  - name: netaddr

  # @pypi [ngxtop](https://pypi.org/project/ngxtop/) - ngxtop parses your nginx access log and outputs useful, top-like, metrics of your nginx server
  - name: ngxtop

  # @pypi [molecule](https://pypi.org/project/molecule/) - Molecule project is designed to aid in the development and testing of Ansible roles. Molecule provides support for testing with multiple instances, operating systems and distributions, virtualization providers, test frameworks and testing scenarios
  - name: molecule

  # @pypi [mycli](https://pypi.org/project/mycli/) - Command line interface for MySQL database with auto-completion and syntax highlighting
  - name: mycli

  # @pypi [pre-commit](https://pypi.org/project/pre-commit/) - A framework for managing and maintaining multi-language pre-commit hooks
  - name: pre-commit

  # @pypi [pywhat](https://pypi.org/project/pywhat/) - A tool that identify what something is, whether it be a file or a text, or even the hex of a file, and text within files - what is recursive
  - name: pywhat

  # @pypi [social-analyzer](https://pypi.org/project/social-analyzer/) - API, CLI & Web App for analyzing & finding a person’s profile across social media websites
  - name: social-analyzer

  # @pypi [spotdl](https://pypi.org/project/spotdl/) - A tool to download Spotify playlists and songs along with album art and metadata
  - name: spotdl

  # @pypi [starred](https://pypi.org/project/starred/) - Generate a GitHub Awesome list directly from your starred repositories
  - name: starred

  # @pypi [statcode](https://pypi.org/project/statcode/) - `man` pages for HTTP status codes (used by running `statcode 418`, for instance)
  - name: statcode

  # @pypi [truffleHog](https://pypi.org/project/truffleHog/) - A tool which makes it easier to search through the history of a git repository to discover passwords and other secrets
  - name: truffleHog

  # @pypi [virtualenv](https://pypi.org/project/virtualenv/) - A tool for creating isolated virtual python environments
  - name: virtualenv

  # @pypi [yamllint](https://pypi.org/project/yamllint/) - A linter for YAML files
  - name: yamllint
