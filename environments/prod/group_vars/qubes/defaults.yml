---
# Set to false to enable debugging (confusing, I know) - comment out variable to turn off
# This var, when set will ignore errors encountered by running some roles and continue to the next role
debug_qubes: false
# Do not include dotfiles with sensitive information by default
include_pii_dotfiles: false
