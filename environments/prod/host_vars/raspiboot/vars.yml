---
# @var nginx_serve_http: true # `nginx_serve_http` is true by default - it is a variable that allows you to disable
# the serving of HTTP (over port 80). This host is using it because a Docker container is using `network_mode: host`
# with port 80 already in use by Docker.
nginx_serve_http: false
