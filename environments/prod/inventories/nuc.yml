---
all:
  children:
    ubuntu-desktop:
      hosts:
        nuc:
          ansible_become_password: '{{ admin_password }}'
          ansible_host: '{{ domains[inventory_hostname].hostname }}'
          ansible_port: '{{ ssh_port }}'
          ansible_password: '{{ admin_password }}'
          ansible_user: '{{ admin_username }}'
