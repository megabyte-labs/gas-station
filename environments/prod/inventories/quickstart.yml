---
all:
  children:
    desktop:
      children:
        nix:
          hosts:
            standard:
              ansible_host: standard
              ansible_password: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
              ansible_user: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
        qubes:
          hosts:
            standard:
              ansible_host: standard
              ansible_password: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
              ansible_user: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
        windows:
          hosts:
            standard:
              ansible_host: standard
              ansible_password: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
              ansible_user: "{{ lookup('env', 'ANSIBLE_PASSWORD') }}"
