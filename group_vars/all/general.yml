authorized_key_file: id_rsa_local.pub      # Default public key to add to authorized_keys

certbot_certs:
  - email: "{{ cloudflare_email }}"
    domains:
      - "*.megabyte.space"
      - "*.home.megabyte.space"
      - "*.lab.megabyte.space"

docker_users:
  - "{{ ansible_user }}"

https_repository_prefix: http://HTTPS///   # Repository URLs will start with this string - this makes HTTPS calls to apt-cacher-ng cachable

netdata_rooms:                             # A map of netdata rooms that are joinable (generate your own keys on https://app.netdata.cloud/ and also get your API key for this feature to work)
  do: 0f7a2d28-77c0-4eb1-970b-22405a3886f7
  general: fb8e46ae-4354-454a-b676-46cda89c2e9b
  james: 495e99ef-60b4-43a4-bb60-4e05accf58a2
netdata_room: "{{ netdata_rooms.general }}" # Default netdata.cloud room to join

nodejs_version: "12.x"                     # The version of NodeJS to install

security_autoupdate_mail_to: "help@megabyte.space"
security_default_user: "hawkwood"
security_ssh_port: "2214"
security_sudoers_passworded:
  - "{{ ansible_user }}"
