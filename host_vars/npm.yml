docker_compose_task: git@gitlab.com:ProfessorManhattan/nginx-Proxy-Manager.git

hostname: ProxyManager

netdata_room: "{{ netdata_rooms.james }}"

pips:
  - name: docker-compose

snaps:
  - name: docker

ssh_private_keys:
  - id_rsa_web