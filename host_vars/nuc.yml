certbot_certs:
  - email: "{{ certbot_cloudflare_email }}"
    domains:
      - "*.lab.megabyte.space"

graylog_email_from_email: help@megabyte.space
graylog_fqdn: https://graylog.lab.megabyte.space

hostname: NUC

install_maas: yes

netdata_room: "{{ netdata_rooms.james }}"

nginx_sites_available:
  - graylog.conf
  - maas.conf

nginx_sites_enabled:
  - graylog.conf
  - maas.conf

reinstall_maas_database: no

ssh_private_keys:
  - id_rsa_web