all:
  children:
    pfsense:
      hosts:
        brock:
          ansible_host: pfSense
          ansible_port: 2214
          ansible_password: "{{ vault_linux_password }}"
          ansible_user: root
          ansible_sudo_pass: "{{ vault_linux_password }}"