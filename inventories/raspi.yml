all:
  children:
    raspberry:
      hosts:
        raspi:
          ansible_host: Raspi
          ansible_port: 2214
          ansible_password: "{{ raspi_password }}"
          ansible_user: "{{ raspi_username }}"
          ansible_sudo_pass: "{{ raspi_password }}"