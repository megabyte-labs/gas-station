all:
  children:
    raspberry:
      hosts:
        raspi:
          ansible_host: Raspi
          ansible_port: 2214
          ansible_password: "{{ vault_linux_password_aux }}"
          ansible_user: "{{ vault_linux_username_aux }}"
          ansible_sudo_pass: "{{ vault_linux_password_aux }}"