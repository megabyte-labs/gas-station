all:
  children:
    raspberry:
      hosts:
        raspiauth:
          ansible_connection: ssh
          ansible_host: RaspiAuth
          ansible_port: 2214
          ansible_password: "{{ raspiauth_password }}"
          ansible_user: "{{ raspiauth_username }}"
          ansible_sudo_pass: "{{ raspiauth_password }}"