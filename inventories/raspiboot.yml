all:
  children:
    raspberry:
      hosts:
        raspiboot:
          ansible_connection: ssh
          ansible_host: RaspiBoot
          ansible_port: 2214
          ansible_password: "{{ raspiboot_password }}"
          ansible_user: "{{ raspiboot_username }}"
          ansible_sudo_pass: "{{ raspiboot_password }}"