all:
  children:
    raspberry:
      hosts:
        raspiproxy:
          ansible_connection: ssh
          ansible_host: RaspiProxy
          ansible_port: 2214
          ansible_password: "{{ raspiproxy_password }}"
          ansible_user: "{{ raspiproxy_username }}"
          ansible_sudo_pass: "{{ raspiproxy_password }}"