all:
  children:
    raspberry:
      hosts:
        raspitor:
          ansible_connection: ssh
          ansible_host: RaspiTor
          ansible_port: 2214
          ansible_password: "{{ raspitor_password }}"
          ansible_user: "{{ raspitor_username }}"
          ansible_sudo_pass: "{{ raspitor_password }}"