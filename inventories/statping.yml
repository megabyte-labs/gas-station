all:
  children:
    ubuntu-server:
      hosts:
        raspi:
          ansible_host: StatPing
          ansible_port: 2214
          ansible_password: "{{ statping_password }}"
          ansible_user: "{{ statping_username }}"
          ansible_sudo_pass: "{{ statping_password }}"