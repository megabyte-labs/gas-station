all:
  children:
    ubuntu-server:
      hosts:
        statuscheck:
          ansible_host: StatusCheck
          ansible_port: 2214
          ansible_password: "{{ statuscheck_password }}"
          ansible_user: "{{ statuscheck_username }}"
          ansible_sudo_pass: "{{ statuscheck_password }}"