all:
  children:
    ubuntu-desktop:
      hosts:
        workstation:
          ansible_connection: local
          ansible_password: "{{ vault_linux_password }}"
          ansible_sudo_pass: "{{ vault_linux_password }}"
          ansible_user: "{{ vault_linux_username }}"
