all:
  children:
    raspberry:
      hosts:
        instagram:
          ansible_host: 192.168.30.69
          ansible_password: "{{ vault_linux_password_aux }}"
          ansible_user: ubuntu
        pihole:
          ansible_host: 192.168.30.14
          ansible_password: "{{ vault_linux_password_aux }}"
          ansible_user: ubuntu
    maas:
      hosts:
        maas:
          ansible_host: 10.24.69.5
          ansible_password: "{{ vault_linux_password }}"
          ansible_user: "{{ vault_linux_username }}"
    pfsense:
      hosts:
        brock:
          ansible_host: 10.24.69.1
          ansible_password: "{{ vault_linux_password_pfsense }}"
          ansible_user: root
    ubuntu-full:
      hosts:
        nuc:
          ansible_host: 192.168.81.14
          ansible_password: "{{ vault_linux_password }}"
          ansible_user: "{{ vault_linux_username }}"