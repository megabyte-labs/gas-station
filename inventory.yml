all:
  children:
    instagram-bot:
      hosts:
        amber.megabyte.home:
          ansible_host: 192.168.14.5
          ansible_password: "{{ vault_linux_password_aux }}"
    maas:
      hosts:
        maas.megabyte.home:
          ansible_host: 10.24.69.5
          ansible_password: "{{ vault_linux_password }}"
    pfsense:
      hosts:
        pfsense.megabyte.home:
          ansible_host: 10.24.69.1
          ansible_password: "{{ vault_linux_password_pfsense }}"
    pihole:
      hosts:
        pihole.megabyte.home:
          ansible_host: 192.168.1.5
          ansible_password: "{{ vault_linux_password_aux }}"
    selks:
      hosts:
        selks-1.megabyte.home:
          ansible_host: 10.24.69.6
          ansible_password: "{{ vault_linux_password }}"
    software-proxy:
      hosts:
        software.megabyte.home:
          ansible_host: 192.168.14.14
          ansible_password: "{{ vault_linux_password }}"
    ubuntu-full:
      hosts:
        sage.megabyte.home:
          ansible_connection: local
          inventory_hostname: sage
    windows-base:
      hosts:
        maz.megabyte.home:
          ansible_connection: winrm
          ansible_host: 10.24.69.7
          ansible_password: "{{ vault_windows_password }}"
          ansible_user: "{{ vault_windows_username }}"
          ansible_become_method: runas
          ansible_become_pass: "{{ vault_windows_password }}"
        brock.megabyte.home:
          ansible_connection: winrm
          ansible_host: 192.168.14.69
          ansible_password: "{{ vault_windows_password }}"
          ansible_user: "{{ vault_windows_username }}"
          ansible_become_method: runas
          ansible_become_pass: "{{ vault_windows_password }}"
    windows-full:
      hosts:
        devy.megabyte.home:
          ansible_connection: winrm
          ansible_host: 10.24.69.8
          ansible_password: "{{ vault_windows_password }}"
          ansible_user: "{{ vault_windows_username }}"
          ansible_become_method: runas
          ansible_become_pass: "{{ vault_windows_password }}"