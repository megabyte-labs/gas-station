---
- hosts: all
  roles:
    - ../roles/tools/awscli # AWS CLI: A command-line interface for AWS
    - ../roles/tools/azurecli # Azure CLI: A command-line interface for working with Microsoft Azure
    - ../roles/tools/googlecloudsdk # GCP SDK: Command-line tools for working with Google Cloud Platform
    - ../roles/tools/wpcli # WP-CLI: A CLI for working with WordPress
    - ../roles/tools/s5cmd # s5cmd: The fastest tool for interacting with S3 buckets
    - ../roles/services/goofys #goofys: Mount an S3 bucket as a filey system
