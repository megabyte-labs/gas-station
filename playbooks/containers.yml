---
- hosts: all
  roles:
    - ../roles/virtualization/dive # dive: A tool for exploring a docker image
    - ../roles/virtualization/dockerslim # Docker Slim: Toolset for generating compact, secure Docker images
    - ../roles/virtualization/gvisor # gvisor: Makes it simple to run sandboxed containers
    - ../roles/virtualization/kubernetes # Kubernetes: Tools useful for managing Kubernetes clusters
    - ../roles/virtualization/swarm
    - ../roles/virtualization/dockerpushrm # dockerpushrm - Push README to Contianer Registry
