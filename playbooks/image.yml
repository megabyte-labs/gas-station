---
- hosts: all
  roles:
    - ../roles/applications/etcher # Etcher: Creates bootable USBs
    - ../roles/applications/raspberryimager # Raspberry Imager: Utility that flashes MicroSD cards for use with Raspberry Pis
