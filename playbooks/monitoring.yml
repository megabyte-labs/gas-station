---
- hosts: all
  roles:
    - ../roles/services/elasticagent
    - ../roles/services/netdata
    - ../roles/services/wazuh
    - ../roles/tools/nmap # nmap: A network mapping free security scanner
    - ../roles/tools/watchman # Facebook Watchman: Watches file system and can trigger commands
    - ../roles/tools/sysdig # sysdig: System-level exploration and troubleshooting tool
    - ../roles/applications/wireshark # Wireshark: Most widely-used network protocol analyzer (packet sniffer)
