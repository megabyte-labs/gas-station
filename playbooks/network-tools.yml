---
- hosts: all
  roles:
    - ../roles/tools/nmap # nmap: A network mapping free security scanner
    - ../roles/applications/wireshark # Wireshark: Most widely-used network protocol analyzer (packet sniffer)
    - ../roles/tools/ngrok # Ngrok: Secure introspectable tunnels to localhost
    - ../roles/tools/mitmproxy # MITM Proxy: Tool for simulating MITM scenarios
    - ../roles/services/cloudflare
    - ../roles/system/warp
    - ../roles/applications/snitch # Snitch: Application firewall
