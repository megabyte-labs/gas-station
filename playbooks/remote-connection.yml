---
- hosts: all
  roles:
    - ../roles/applications/remotedesktop # Remote Desktop: Installs the best remote desktop software based on the operating system
    - ../roles/tools/teleport # Teleport Client: Client required to join a Teleport network
    - ../roles/tools/windowsadmincenter # Windows Admin Center: Browser-based app for managing Windows servers
    - ../roles/services/guacamole # Guacamole Client: Client for hosting RDP from browser
