---
- hosts: all
  roles:
    - ../roles/applications/flameshot
    - ../roles/applications/sharex
    - ../roles/tools/scrcpy
    - ../roles/tools/trec # t-rec: Blazingly fast terminal recorder that generates animated gif images for the web written in rust
