---
services_ansible_user: "{{ ansible_user }}"
samba_netbios_name: home

services_samba_users:
  - name: test
    password: password
    samba_password: password
    uid: '449'
    groups:
      - sambausers
      - lpadmin
