---
- name: Power Off VMs
  win_hyperv_guest:
    name: "{{ hyperv_vm_name }}"
    state: poweroff

- name: Delete VMs
  win_hyperv_guest:
    name: "{{ hyperv_vm_name }}"
    state: absent

- name: Remove VHDX files
  win_file:
    path: "{{ hyperv_vhdx_dest }}"
    state: absent