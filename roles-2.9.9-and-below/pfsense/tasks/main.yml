---
- name: Install packages from the FreeBSD repository
  pkgng:
    name: "{{ pfsense_packages }}"
    state: present

- name: Install custom pkg adds
  pkgng:
    name: "{{ item }}"
    state: present
  with_items: "{{ pfsense_custom_packages }}"