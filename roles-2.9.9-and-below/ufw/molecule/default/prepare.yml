---
- name: Setup Archlinux client
  hosts: ufw-archlinux
  gather_facts: false
  tasks:
    - name: Refresh pacman package database
      raw: pacman -Sy

    - name: Install python3
      raw: pacman -S --noconfirm python3
