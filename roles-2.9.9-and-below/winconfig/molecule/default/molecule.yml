---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: libvirt
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: win10
    box: peru/windows-10-enterprise-x64-eval
    memory: 2048
    cpus: 1
provisioner:
  name: ansible
verifier:
  name: ansible
