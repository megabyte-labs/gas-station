---
default_browser: "none" # Set this to 'chrome' if you would like the Chromium browser to be set as default
linux_install_chrome_gnome_shell: true # On Linux, this will install the native host connector necessary for the Chrome GNOME Extension to work
