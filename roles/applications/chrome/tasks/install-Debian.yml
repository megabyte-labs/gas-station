---
- name: Install Chromium
  snap:
    name: chromium 
    state: present

- name: Set Chromium as the default browser
  alternatives:
    name: x-www-browser
    link: /usr/bin/x-www-browser
    path: /snap/bin/chromium
    priority: 200
  when: default_browser == 'chrome'
