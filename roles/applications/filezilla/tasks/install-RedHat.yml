---
# @error TASK [roles/applications/filezilla : Ensure FileZilla is installed] ************
# fatal: [workstation]: FAILED! => {"changed": false, "msg": "Unsupported parameters for (ansible.legacy.dnf) module: enable_repo. Supported parameters include: allow_downgrade, disablerepo, conf_file, security, state, releasever, autoremove, lock_timeout, enablerepo, bugfix, nobest, update_cache (expire-cache), cacheonly, disable_plugin, validate_certs, name (pkg), enable_plugin, allowerasing, skip_broken, download_only, exclude, install_weak_deps, installroot, disable_excludes, update_only, list, download_dir, install_repoquery, disable_gpg_check."}
- name: "Ensure {{ app_name }} is installed"
  yum:
    name: filezilla
    state: "{{ app_state | default('present') }}"
