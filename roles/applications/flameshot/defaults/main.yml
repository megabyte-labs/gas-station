---
user_configs:
  - username: "{{ ansible_user | default(lookup('env', 'USER')) }}"
# Example config (note that root user is skipped because it does not have a directory in /home/)
# user_configs:
#   - username: "{{ ansible_user | default(lookup('env', 'USER')) }}"
#   - username: root
