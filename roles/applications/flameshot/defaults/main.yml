---
# @var user_configs: []
# A list of locations to import the user configs for configuring Flameshot
user_configs:
  - username: "{{ ansible_user | default(lookup('env', 'USER')) }}"
# @example #
# user_configs:
#   - username: "{{ ansible_user | default(lookup('env', 'USER')) }}"
#   - username: root
# @end
