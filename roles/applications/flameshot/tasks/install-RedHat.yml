---
- name: Install flameshot
  dnf:
    name: flameshot
    state: present

- name: Add Flameshot configuration to users
  include_tasks: config-Linux.yml
  loop: "{{ user_configs }}"
  loop_control:
    label: "{{ user.username }}"
    loop_var: user
