---
- name: Add .hyper.js to user's home directory
  become: true
  become_user: "{{ user }}"
  copy:
    src: .hyper.js
    dest: ~/.hyper.js
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: 0600
