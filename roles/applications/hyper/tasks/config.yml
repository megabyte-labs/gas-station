---
- name: Add .hyper.js to user's home directory
  become: yes
  become_user: "{{ item }}"
  copy:
    src: .hyper.js
    dest: ~/.hyper.js
    owner: "{{ item }}"
    group: "{{ item }}"
    mode: "600"
