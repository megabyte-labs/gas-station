---
- name: Install Lollypop apt repository
  apt_repository:
    repo: "{{ lollypop_apt_repository }}"
    filename: lollypop
    state: present
    update_cache: yes

- name: Install Lollypop
  apt:
    name: lollypop
    state: present
