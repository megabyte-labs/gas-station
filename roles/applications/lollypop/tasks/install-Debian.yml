---
- name: Install Lollypop apt repository
  apt_repository:
    repo: "{{ lollypop_apt_repo }}"
    filename: lollypop
    state: present
    update_cache: true

- name: Install Lollypop
  apt:
    name: lollypop
    state: "{{ app_state | default('present') }}"
