---
- name: Include variables based on OS Family 
  include_vars: "{{ ansible_facts['os_family'] }}.yml"

- name: Include tasks based on OS Family
  become: yes # TODO: Make this use install-Linux.yml instead of install-Debian.yml because the process for all Linux distros is the same
  include_tasks: "install-{{ ansible_facts['os_family'] }}.yml"
