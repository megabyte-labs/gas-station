---
test:python:
  allow_failure: true
  artifacts:
    reports:
      cobertura: coverage.xml
      junit: report.xml
    when: always
  cache:
    key: $CI_COMMIT_REF_SLUG
    paths:
      - .cache/
  image:
    name: megabytelabs/pytest:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - 'test/**/*.py'
  script:
    - task python:test:ci
  stage: test
  variables:
    GIT_DEPTH: 1
