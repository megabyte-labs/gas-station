---
- name: Install Flameshot
  apt:
    name: flameshot
    state: present

- name: Add Flameshot configuration to users
  become: yes
  become_user: "{{ item }}"
  include_tasks: flameshot-user-config.yml
  with_items: "{{ flameshot_users }}"
