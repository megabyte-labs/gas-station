# https://repo.skype.com/rpm/stable/
- name: Add Skype repo 
  copy:
    src: skype-stable.repo
    dest: "{{ repo_dir }}"

- name: Install Skype 
  dnf:
    name: skypeforlinux
    state: latest