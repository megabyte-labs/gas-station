- name: Install wireshark for Archlinux 
  pacman:
    name: "{{ wireshark_packages }}"
    state: latest

# The installation failed on Arch because of the following error:
# Failed to install wireshark-qt wireshark-cli, warning: dependency cycle detected