---
galaxy_info:
  author: ProfessorManhattan
  description: Install Xcode on Mac OS X
  company: Megabyte Labs
  # issue_tracker_url: http://example.com/issue/tracker
  license: license (MIT)

  min_ansible_version: 2.10

  platforms:
    - name: Mac OS X
      versions:
        - 10.15

  galaxy_tags:
    - xcode
    - ide
    - code
    - editor
    - ios
    - apple

dependencies:
  - role: mas
    when: ansible_os_family == 'Darwin'
