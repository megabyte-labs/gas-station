--- # TODO: Add code from https://github.com/macstadium/ansible-role-xcode
# The code in the link above does stuff like automatically accept the license
- name: Register the MAS installed apps
  command: mas list
  register: mas_list
  changed_when: false

- name: "Ensure {{ xcode_mas_name }} is installed"
  command: "mas install '{{ xcode_mas_id }}'" # TODO: Add args: creates: here
  when: (xcode_mas_id | string) not in mas_list.stdout
