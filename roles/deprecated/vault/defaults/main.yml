---
https_repository_prefix: https:// # This is for apt repositories. Change this to http://HTTPS/// if you are using AptCacherNG.

# Specify the users to perform the Homebrew install with (for Mac OS X only)
vault_user: "{{ ansible_user | default(lookup('env', 'USER')) }}"
