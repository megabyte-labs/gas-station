---
dmg_installs: []
# Example dmg_installs
#dmg_installs:
#  - name:
#    url:
#  - name:
#    url:

force_reinstall: no # If the application is already in the Applications folder, then this option will delete the existing app and re-install it if the value is set to yes
remove_after_install: yes # Whether or not to remove the DMG file after the installation completes
tmp_dir: /tmp # The temporary directory in which to store the DMG file
