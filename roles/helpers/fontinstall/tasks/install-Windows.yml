---
- name: "Ensure {{ app_name }} is installed"
  ansible.windows.win_command: "npm install -g google-font-installer@latest"

- name: Ensure fonts are installed
  ansible.windows.win_command: "gfi install {{ font }}"
  loop: "{{ fonts }}"
  loop_control:
    label: "{{ font }}"
    loop_var: font
