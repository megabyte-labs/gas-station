---
fix:black:
  image:
    name: megabytelabs/yamllint:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.py'
  script:
    - set -eo pipefail
    - yamllint -c .config/yamllint.yml -s .
  stage: fix
  variables:
    GIT_DEPTH: 1
