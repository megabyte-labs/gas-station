---
lint:black:
  image:
    name: megabytelabs/black:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.py'
  script:
    - set -eo pipefail
    - black . --check
  stage: lint
  variables:
    GIT_DEPTH: 1
