---
lint:flake8:
  image:
    name: megabytelabs/flake8:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.py'
  script:
    - set -eo pipefail
    - flake8 .
  stage: lint
  variables:
    GIT_DEPTH: 1
