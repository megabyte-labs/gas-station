---
lint:stylelint:
  image:
    name: megabytelabs/stylelint:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.{css,scss}'
  script:
    - set -eo pipefail
    - stylelint "**/*.(css|scss)"
  stage: lint
  variables:
    GIT_DEPTH: 1
