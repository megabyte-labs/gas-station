---
# Set java_packages if you would like to use a different version than the
# default for the OS (see defaults per OS in `vars` directory). This will
# not apply to Windows installations.
# java_packages: []

# @var java_home: ""
# Sets JAVA_HOME variable for Linux-based installations
java_home: '' # Used for Linux-based installs
