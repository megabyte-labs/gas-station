---
- name: Setup Archlinux container
  hosts: archlinux
  gather_facts: false

  tasks:
    - name: Install python3 on ArchLinux container
      block:
        - name: Refresh pacman package database
          raw: pacman -Sy
        - name: Install python3
          raw: pacman -S --noconfirm python3 sudo
