---
- name: Ensure nginx is installed
  homebrew:
    name: nginx
    state: present

- name: Ensure PHP packages are installed
  homebrew:
    name: "{{ php_packages }}"
    state: present
    install_options: without-apache