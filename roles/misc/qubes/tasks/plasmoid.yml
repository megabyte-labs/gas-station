---
- name: "Copy the {{ item }} Plasmoid to a temporary location"
  become: true
  copy:
    src: "files/plasmoids/{{ item }}"
    dest: "/tmp/{{ item }}"

- name: "Install the {{ item }} Plasmoid"
  become: true
  command: "plasmapkg -u /tmp/{{ item }}"
