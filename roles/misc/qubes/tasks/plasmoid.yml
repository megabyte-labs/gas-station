---
- name: "Copy the {{ item | basename }} Plasmoid to a temporary location"
  become: true
  copy:
    src: "{{ item }}"
    dest: "/tmp/{{ item | basename }}"

- name: "Install the {{ item | basename }} Plasmoid"
  become: true
  command: "plasmapkg -u /tmp/{{ item | basename }}"
