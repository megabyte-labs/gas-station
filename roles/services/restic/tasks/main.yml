---
- name: Include variables based on OS Family 
  include_vars: "{{ ansible_os_family }}.yml"

- name: Include tasks based on OS Family
  become: yes
  include_tasks: "install-{{ ansible_os_family }}.yml"

- name: Install the docker-compose-backup executable # TODO: Make this run only when system is not Windows or figure out how the script can run on Windows
  become: yes
  include_tasks: docker-compose-backup.yml
