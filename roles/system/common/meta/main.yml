---
galaxy_info:
  role_name: common
  author: ProfessorManhattan
  description: Performs initialization tasks
  company: Megabyte Labs
  issue_tracker_url: https://gitlab.com/megabyte-labs/ansible-roles/common/-/issues
  license: license (MIT)
  min_ansible_version: 2.10
  platforms:
    - name: Ubuntu
      versions:
        - focal
  galaxy_tags:
    - meta
    - initialization
    - grub
    - plymouth
    - boot
    - themes
  namespace: professormanhattan
dependencies:
  - role: professormanhattan.homebrew
    when: ansible_os_family == "Darwin"
