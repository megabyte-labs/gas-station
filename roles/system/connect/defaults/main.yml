---
# When this is set to true, configurations will be generated for double-proxying to the destination
nginx_proxy_host: false

# @var password_salt: ""
# Change this random string for added security - it is used to hash the ansible_user's password so it is not sent in clear text
# Ensure the the length of this string is <=16 characters
password_salt: SkibadeeDoo337Xm

# @var replace_ubuntu_user_with_ansible_user: true
# Set this variable to true if the default ubuntu user should be removed and `ansible_user` be created
replace_ubuntu_user_with_ansible_user: true
