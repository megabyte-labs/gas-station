---
- name: Clone AppImages from repository
  git:
    repo: "{{ app_images_repository }}"
    dest: ~/Applications
    depth: "1"
    force: yes

- name: Make Applications executable
  file:
    path: ~/Applications
    owner: "{{ item }}"
    group: "{{ item }}"
    mode: "700"
    recurse: yes
