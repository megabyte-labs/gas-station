---
# fatal: [workstation]: FAILED! => {"ansible_facts": {}, "ansible_included_var_files": [], "changed": false, "message": "Could not find or access 'RedHat.yml'\nSearched in:\n\t/home/hawkwood/Playbooks/roles/system/desktop/vars/RedHat.yml\n\t/home/hawkwood/Playbooks/roles/system/desktop/RedHat.yml\n\t/home/hawkwood/Playbooks/roles/system/desktop/tasks/vars/RedHat.yml\n\t/home/hawkwood/Playbooks/roles/system/desktop/tasks/RedHat.yml\n\t/home/hawkwood/Playbooks/vars/RedHat.yml\n\t/home/hawkwood/Playbooks/RedHat.yml on the Ansible Controller.\nIf you are using a module and expect the file to exist on the remote, see the remote_src option"}
# This step includes making the ~/Applications folder link AppImages so they show up in the launcher
- name: Include variables based on the operating system
  include_vars: "{{ ansible_os_family }}.yml"

- name: Include tasks based on the operating system
  become: true
  become_method: "{{ 'runas' if ansible_os_family == 'Windows' else 'sudo' }}"
  become_user: "{{ ansible_user if ansible_os_family == 'Windows' else omit }}"
  block:
    - include_tasks: "setup-{{ ansible_os_family }}.yml"
