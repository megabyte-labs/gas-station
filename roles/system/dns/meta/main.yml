---
galaxy_info:
  role_name: dns
  author: ProfessorManhattan
  description: Sets up encrypted DNS
  company: Megabyte Labs
  issue_tracker_url: https://gitlab.com/megabyte-labs/ansible-roles/dns/-/issues
  license: license (MIT)
  min_ansible_version: 2.10
  platforms:
    - name: Ubuntu
      versions:
        - focal
  galaxy_tags:
    - cloudflare
    - dns
    - dnsovertls
    - resolved
    - systemd
    - systemdresolved
    - tls
  namespace: professormanhattan
dependencies:
  - role: professormanhattan.homebrew
    when: ansible_os_family == "Darwin"
