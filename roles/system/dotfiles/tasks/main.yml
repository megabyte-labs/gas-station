---
- name: Loop through users and add dotfiles
  become: yes
  become_user: "{{ user.username }}"
  include_tasks: user-Linux.yml # TODO: Make sure system is actually Linux
  loop: "{{ user_ssh_configs }}"
  loop_control:
    loop_var: user
