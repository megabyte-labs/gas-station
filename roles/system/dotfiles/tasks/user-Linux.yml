---
# TODO: Add dotfiles repo

- name: Add dynamic dotfiles
  template:
    src: "{{ item }}.j2"
    dest: "~/{{ item }}"
    owner: "{{ user.username }}"
    group: "{{ user.group | default(omit) }}"
    mode: 0600
  loop:
    - .gitconfig
    - .netrc
    - .npmrc
    - .tinypng
