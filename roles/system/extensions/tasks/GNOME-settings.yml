---
- name: Apply extension settings # TODO: Only run the command when the value needs to be changed
  become: true
  become_user: "{{ user.username }}"
  command: "{{ item }}"
  with_items: "{{ item.settings | default([]) }}"
