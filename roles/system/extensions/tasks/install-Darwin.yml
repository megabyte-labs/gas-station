---
- name: Ensure qlplugins are installed
  become: false
  community.general.homebrew_cask:
    name: "{{ plugin }}"
    state: present
  ignore_errors: true
  loop: "{{ qlplugins }}"
  loop_control:
    label: "{{ plugin }}"
    loop_var: plugin
