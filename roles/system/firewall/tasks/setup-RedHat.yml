---
- name: Import EPEL GPG key
  rpm_key:
    key: https://dl.fedoraproject.org/pub/epel/RPM-GPG-KEY-EPEL-8
    state: present
  when: ansible_distribution != 'Fedora'

- name: Enable EPEL
  dnf:
    name: https://dl.fedoraproject.org/pub/epel/epel-release-latest-8.noarch.rpm
    state: present
  when: ansible_distribution != 'Fedora'
# @error on Fedora 35 - fatal: [workstation]: FAILED! => {"changed": false, "failures": [], "msg": "Depsolve Error occurred: \n Problem: conflicting requests\n  - nothing provides redhat-release >= 8 needed by epel-release-8-13.el8.noarch", "rc": 1, "results": []}

- name: Run UFW tasks
  include_tasks: ufw.yml
