---
galaxy_info:
  role_name: fuse
  author: ProfessorManhattan
  description: Installs macFUSE on macOS
  company: Megabyte Labs
  issue_tracker_url: https://gitlab.com/megabyte-labs/ansible-roles/fuse/-/issues
  license: license (MIT)
  min_ansible_version: 2.10
  platforms:
    - name: MacOS
      versions:
        - all
  galaxy_tags:
    - macosx
    - fuse
    - filesystem
  namespace: professormanhattan
dependencies:
  - role: professormanhattan.homebrew
    when: ansible_os_family == "Darwin"
