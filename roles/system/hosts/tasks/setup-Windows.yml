---
- name: Set up etc/hosts file # TODO: This will not work with new hosts variable
  win_lineinfile:
    path: C:\Windows\System32\Drivers\etc\hosts
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
    state: present
  with_items: "{{ etc_hosts_list }}"