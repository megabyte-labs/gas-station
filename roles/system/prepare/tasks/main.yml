---
- name: Ensure apt locks are cleared
  become: true
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /var/lib/apt/lists/lock
    - /var/cache/apt/archives/lock
    - /var/lib/dpkg/lock
  when:
    - "'windows' not in group_names"
