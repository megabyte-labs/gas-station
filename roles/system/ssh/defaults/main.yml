---
user_configs:
  - username: "{{ ansible_user }}"
    authorized_keys:
      - "{{ lookup('file', 'id_rsa.pub') }}"
    private_keys:
      - name: id_rsa
        path: files/keys/id_rsa
  - username: root
    private_keys:
      - name: id_rsa
        path: files/keys/id_rsa
