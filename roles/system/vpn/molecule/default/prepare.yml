---
- name: Create system user for testing
  hosts: all
  gather_facts: false
  tasks:
    - name: Create {{ vpn_ansible_user }}
      user:
        name: "{{ vpn_ansible_user }}"
        comment: VPN Test User
        create_home: yes
        state: present
