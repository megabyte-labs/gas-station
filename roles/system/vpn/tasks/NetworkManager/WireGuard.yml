---
- name: Copy WireGuard nmconnection file
  copy:
    src: "files/vpn/{{ item.file }}"
    dest: "{{ system_connections_path }}/{{ item.file }}"
    owner: root
    group: root
    mode: "0600"
  notify: restart networkmanager
