---
- name: "Ensure {{ app_name }} is installed"
  yum:
    name: aria2
    state: present
    enable_repo: epel

- name: Ensure AriaNG is installed
  include_tasks: ariang-Linux.yml
  when: install_ariang
