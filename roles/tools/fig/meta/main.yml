---
galaxy_info:
  role_name: fig
  author: ProfessorManhattan
  description: Installs Fig (a tool that adds autocomplete to your terminal) on MacOS
  company: Megabyte Labs
  issue_tracker_url: https://gitlab.com/megabyte-labs/ansible-roles/fig/-/issues
  license: license (MIT)
  min_ansible_version: 2.10
  platforms:
    - name: MacOS
      versions:
        - all
  galaxy_tags:
    - autocomplete
    - terminal
    - shell
    - iterm
  namespace: professormanhattan
dependencies:
  - role: professormanhattan.homebrew
    when: ansible_os_family == 'Darwin'
