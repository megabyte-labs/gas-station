---
- name: "Ensure {{ app_name }} is installed"
  become: false
  community.general.homebrew:
    name: fzf
    state: "{{ app_state | default('present') }}"

- name: "Ensure key bindings and fuzzy completion is enabled" # noqa 301
  command: $(brew --prefix)/opt/fzf/install # TODO Figure out what file this creates and add args -> creates: filename and remove noqa 301
