---
- name: Ensure ~/.config/ghorg directory exists
  become_user: "{{ user.username }}"
  file:
    dest: ~/.config/ghorg
    state: directory
    mode: 0700
# TASK [roles/tools/ghorg : Ensure ghorg is setup] ****************************************************************************************************
# An exception occurred during task execution. To see the full traceback, use -vvv. The error was: ansible.errors.AnsibleUndefinedVariable: 'dict object' has no attribute 'github_token'
fatal: [workstation]: FAILED! => {"changed": false, "msg": "AnsibleUndefinedVariable: 'dict object' has no attribute 'github_token'"}
- name: "Ensure {{ app_name }} is setup"
  become_user: "{{ user.username }}"
  template:
    src: conf.yaml.j2
    dest: ~/.config/ghorg/conf.yaml
    mode: 0700
