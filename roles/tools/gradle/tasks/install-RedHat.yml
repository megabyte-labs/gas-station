---
# fatal: [workstation]: FAILED! => {"changed": false, "failures": ["No package gradle available."], "msg": "Failed to install some of the specified packages", "rc": 1, "results": []}
# Not sure why but it did not complain about enablerepo: epel here so I may have deleted when not necessary in some spots
- name: "Ensure {{ app_name }} is installed"
  yum:
    name: gradle
    state: present
    enablerepo: epel
  when: false
