---
- name: Install Homebrew if not already installed
  become: true
  become_user: "{{ homebrew_install_user }}"
  shell: |
    set -o pipefail && echo | /bin/bash -c "$(curl -fsSL {{ homebrew_install_script_url }})"
  args:
    creates: "{{ homebrew_bin_path }}"
  environment:
    CI: 1
