---
galaxy_info:
  role_name: httpie
  author: ProfessorManhattan
  description: Installs HTTPie (a user-friendly, command-line HTTP client) on nearly any platform
  company: Megabyte Labs
  issue_tracker_url: https://gitlab.com/megabyte-labs/ansible-roles/httpie/-/issues
  license: license (MIT)
  min_ansible_version: 2.10
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - curl
    - replacement
    - httpie
    - post
    - get
  namespace: professormanhattan
dependencies:
  - role: professormanhattan.homebrew
    when: ansible_os_family == "Darwin"
