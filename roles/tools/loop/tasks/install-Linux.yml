---
# This should be moved to the generic software list
# Deprecate this role as well
# The brew can just be installed with the long address instead of installing the tap first (e.g. brew install repo/postfix/role_name)
# Please perform this for all roles that can be deprecated - only keep roles that need customization that can't be accommodated by the generic installer
- name: "Ensure {{ app_name }} is installed"
  community.general.snap:
    name: loop-rs
    state: "{{ app_state | default('present') }}"
    channel: beta
