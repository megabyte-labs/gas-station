---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Check packer
    shell: |
      packer --help
    register: this
    failed_when: '"Usage: packer" not in this.stdout'

  - name: Assertion
    assert:
      that: '"Usage: packer" in this.stdout'
