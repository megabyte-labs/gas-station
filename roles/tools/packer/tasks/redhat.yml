- name: Add packer repo
  yum_repository:
    name: hashicorp
    description: Hashicorp repo
    baseurl: "{{ packer_yum_repo }}"
    gpgkey: "{{ packer_gpgkey_url }}"
    gpgcheck: yes

- name: Install packer
  yum:
    name: "{{ package }}"
    state: latest
    update_cache: yes
