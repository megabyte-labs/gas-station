---
user_configs:
  - username: "{{ ansible_user | default(lookup('env', 'USER')) }}"
    # See https://github.com/erroneousboat/slack-term/wiki#running-slack-term-without-legacy-tokens to generate a token
    slack_api_key: ""
