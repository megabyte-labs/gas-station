---
- name: Update apt cache and ensure snapd is installed
  apt:
    name: snapd
    state: present
    update_cache: yes

- name: Ensure the snap core package is installed
  snap:
    name: core
    state: present
