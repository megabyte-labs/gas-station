---
# fatal: [workstation]: FAILED! => {"changed": false, "msg": "Unsupported parameters for (ansible.legacy.dnf) module: enalblerepo. Supported parameters include: enable_plugin, allowerasing, name (pkg), install_repoquery, autoremove, bugfix, disable_plugin, download_only, state, allow_downgrade, releasever, validate_certs, lock_timeout, enablerepo, disable_gpg_check, conf_file, nobest, list, exclude, disable_excludes, security, installroot, download_dir, install_weak_deps, cacheonly, update_only, update_cache (expire-cache), skip_broken, disablerepo."}
- name: "Ensure {{ app_name }} is installed"
  yum:
    name: upx
    state: "{{ app_state | default('latest') }}"
    update_cache: true
#    enalblerepo: "{{ 'epel' if ansible_distribution != 'Fedora' | default(omit) }}"
