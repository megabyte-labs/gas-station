---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
  - name: Check yarn
    shell: |
      yarn --help
    register: this
    failed_when: '"Usage: yarn" not in this.stdout'

  - name: Assertion
    assert:
      that: '"Usage: yarn" in this.stdout'
