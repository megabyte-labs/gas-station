---
- name: Add Yarn's repository
  yum_repository:
    name: yarn
    description: Yarn repo
    baseurl: "{{ yarn_yum_repo }}"

- name: Install Yarn
  yum:
    name: yarn
    state: latest
    update_cache: yes
