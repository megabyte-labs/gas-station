---
- name: Add Yarn's repository
  yum_repository:
    name: Yarn
    description: Yarn repo
    baseurl: "{{ yarn_yum_repo }}"
    gpgkey: "{{ yarn_gpgkey_url }}"
    gpgcheck: yes

- name: Install yarn
  yum:
    name: "{{ package }}"
    state: latest
    update_cache: yes
