---
- name: Add youtube-dl apt repository
  apt_repository:
    filename: youtubedl
    repo: "{{ youtube_dl_apt_repository }}"
    state: present
    update_cache: yes

- name: Install youtube-dl
  apt:
    name: youtube-dl
    state: present
