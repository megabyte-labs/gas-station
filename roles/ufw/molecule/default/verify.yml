---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  tasks:
  - name: Get ufw status
    command: ufw status
    register: ufw_status

  - debug: msg="{{ ufw_status.stdout }}"
