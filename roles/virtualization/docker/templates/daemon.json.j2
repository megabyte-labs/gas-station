{
    hosts = "tcp://0.0.0.0:2376";
    tlsverify = $true;
    tlscacert = "/Users/{{ ansible_user | default(lookup('env', 'USER')) }}/.docker/certs.d/ca.pem";
    tlscert = "/Users/{{ ansible_user | default(lookup('env', 'USER')) }}/.docker/certs.d/docker-server-cert.pem";
    tlskey = "/Users/{{ ansible_user | default(lookup('env', 'USER')) }}/.docker/certs.d/docker-server-key.pem"
}
