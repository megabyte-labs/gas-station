galaxy_info:
  role_name: git-docker
  author: ProfessorManhattan
  description: Sets up docker-compose projects with automatic backups to Wasabi via Restic
  company: Megabyte Labs
  # issue_tracker_url: http://example.com/issue/tracker
  license: license (MIT)

  min_ansible_version: 2.9

  platforms:
    - name: Ubuntu
      versions:
        - focal

  galaxy_tags:
    - git
    - docker
    - automatic
    - backups
    - restic
    - git-cache-meta # noqa 702

dependencies:
  - roles/services/restic
