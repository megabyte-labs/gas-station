---
- name: Ensure kubectl is installed
  community.general.homebrew:
    name: "{{ item }}"
    state: present
  loop:
    - helm
    - hyperkit
    - kompose
    - kubectl
# Run minikube config set driver {{ kubernetes_mac_driver }}
