---
- name: Ensure kubectl is installed
  snap:
    classic: "{{ item.classic | default(false) }}"
    name: "{{ item.name | default(item) }}"
    state: present
  loop:
    - kubectl
    - name: helm
      classic: true
