---
- name: Install Kubernetes tools
  chocolatey.chocolatey.win_chocolatey:
    name: "{{ item }}"
    state: present
  loop:
    - kubernetes-cli
    - kubernetes-helm
    - kubernetes-kompose
# Install minikube and set appropriate driver
