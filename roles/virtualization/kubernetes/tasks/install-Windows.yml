---
- name: Install Kubernetes tools
  chocolatey.chocolatey.win_chocolatey:
    name: "{{ item }}"
    state: present
  with_items:
    - kubernetes-cli
    - kubernetes-helm
    - kubernetes-kompose
