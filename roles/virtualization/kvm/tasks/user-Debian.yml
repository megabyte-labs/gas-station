---
- name: Add specified users to the libvirt and kvm groups
  user:
    name: "{{ user.username }}"
    groups:
      - libvirt
      - kvm
    append: yes
  when:
    - user.kvm_admin
    - user.username
