galaxy_info:
  author: ProfessorManhattan
  description: Installs Parallels on Mac OS X
  company: Megabyte Labs
  # issue_tracker_url: http://example.com/issue/tracker
  license: license (MIT)

  min_ansible_version: 2.9

  platforms:
    - name: Mac OS X
      versions:
        - 10.15

  galaxy_tags:
    - macosx
    - parallels
    - virtualization
    - windows
    - vm

dependencies:
  - role: mas
    when: ansible_os_family == 'Darwin'
