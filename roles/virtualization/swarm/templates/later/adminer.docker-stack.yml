---
version: '3.1'
services:
  adminer:
    container_name: Adminer
    image: adminer:latest
    restart: always
    # volumes:
    #   - ./adminer.css:/var/www/html/adminer.css
    ports:
      - 29919:8080
