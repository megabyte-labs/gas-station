---
- hosts: raspberry:ubuntu-desktop:ubuntu-server:ubuntu-wsl
  vars:
    docker_regenerate_certificates: yes
  tasks:
    - name: Re-generate Docker configuration
      include_tasks: tasks/ubuntu/applications/docker.yml