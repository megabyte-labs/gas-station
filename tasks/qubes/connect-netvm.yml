---
- name: Check the value of {{ item }}'s NetVM
  command: qvm-prefs {{ item }} netvm
  register: connect_netvm

- name: Configure {{ item }}'s NetVM
  command: qvm-prefs --set {{ item }} netvm {{ netvm }}
  when: connect_netvm.stdout != netvm
