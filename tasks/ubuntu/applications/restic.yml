---
- name: Install Restic
  apt:
    name: restic
    state: latest
- name: Install docker-backup script
  template:
    src: docker-compose-backup.j2
    dest: "/usr/bin/docker-compose-backup"
    owner: root
    group: root
    mode: "755"