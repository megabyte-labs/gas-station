---
- name: Set hostname
  hostname:
    name: "{{ hostname }}"
  when: hostname is defined
- name: Set up /etc/hosts file
  lineinfile:
    dest: /etc/hosts
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
    state: present
  with_items: "{{ etc_hosts_list }}"