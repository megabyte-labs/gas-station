---
- name: Install Hyper.js
  become: yes
  apt:
    deb: https://releases.hyper.is/download/deb
- name: Copy .hyper.js to user directory
  copy:
    src: files/dotfiles/.hyper.js
    dest: ~/.hyper.js
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "600"