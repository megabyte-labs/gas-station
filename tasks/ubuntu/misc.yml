---
- name: Set MOTD
  copy:
    src: files/config/motd
    dest: /etc/motd
    owner: root
    group: root
    mode: 0644
- name: Set Chromium as the default browser
  shell: sudo update-alternatives --install /usr/bin/x-www-browser x-www-browser /snap/bin/chromium 200
  when: chromium_default_browser