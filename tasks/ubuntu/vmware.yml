---
- name: Ensure VMWare Workstation Pro is installed (if key is defined)
  tasks:
    - include_role: vmware.vmware-workstation
      vars:
        workstation_license: "{{ vmware_workstation_key }}"
  when: vmware_workstation_key is defined