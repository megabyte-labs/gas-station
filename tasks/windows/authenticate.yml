---
- name: Login to Docker
  win_shell: "docker login -u {{ vault_docker_username }} -p {{ vault_docker_password }}"
  when:
    - vault_docker_password is defined
    - vault_docker_username is defined
