---
- name: Set up etc/hosts file
  win_lineinfile: # TODO
    path: C:\Windows\System32\Drivers\etc\hosts
    regexp: "{{ item.regexp }}"
    line: "{{ item.line }}"
    state: present
  with_items: "{{ etc_hosts_list }}"